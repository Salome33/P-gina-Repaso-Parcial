<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guía Java y Python</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #e0e0e0;
            background: #0a0a0a;
            padding: 20px;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            background: #1a1a1a;
            border-radius: 10px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.5);
            overflow: hidden;
        }

        header {
            background: linear-gradient(135deg, #1e3a8a 0%, #7c2d12 100%);
            color: white;
            padding: 30px;
            text-align: center;
            border-bottom: 3px solid #fbbf24;
        }

        h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        .subtitle {
            font-size: 1.2em;
            opacity: 0.9;
        }

        nav {
            background: #2a2a2a;
            padding: 15px;
            border-bottom: 2px solid #fbbf24;
            position: sticky;
            top: 0;
            z-index: 100;
            text-align: center;
        }

        nav button {
            background: #1e3a8a;
            color: white;
            border: none;
            padding: 10px 20px;
            margin: 5px;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s;
            font-size: 16px;
        }

        nav button:hover {
            background: #fbbf24;
            color: #0a0a0a;
            transform: translateY(-2px);
        }

        .content {
            padding: 30px;
        }

        .section {
            display: none;
            animation: fadeIn 0.5s;
        }

        .section.active {
            display: block;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        h2 {
            color: #fbbf24;
            margin: 30px 0 15px 0;
            padding-bottom: 10px;
            border-bottom: 3px solid #fbbf24;
        }

        h3 {
            color: #60a5fa;
            margin: 20px 0 10px 0;
        }

        .code-block {
            background: #0d1117;
            color: #f8f8f2;
            padding: 20px;
            border-radius: 8px;
            margin: 15px 0;
            overflow-x: auto;
            font-family: 'Courier New', monospace;
            font-size: 14px;
            line-height: 1.5;
            box-shadow: 0 4px 6px rgba(0,0,0,0.3);
            border: 1px solid #30363d;
        }

        .explanation {
            background: #2a2a2a;
            padding: 15px;
            border-left: 4px solid #fbbf24;
            margin: 15px 0;
            border-radius: 5px;
            color: #e0e0e0;
        }

        .keyword { color: #ff79c6; }
        .string { color: #50fa7b; }
        .comment { color: #6272a4; font-style: italic; }
        .function { color: #8be9fd; }
        .number { color: #bd93f9; }

        footer {
            background: #0a0a0a;
            color: white;
            text-align: center;
            padding: 20px;
            border-top: 2px solid #fbbf24;
        }

        .highlight {
            background: #fbbf24;
            color: #0a0a0a;
            padding: 2px 5px;
            border-radius: 3px;
            font-weight: bold;
        }

        .topic-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin: 20px 0;
        }

        .topic-item {
            background: #2a2a2a;
            padding: 20px;
            border-radius: 8px;
            border: 1px solid #3a3a3a;
        }

        .side-by-side {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 20px;
            margin: 20px 0;
        }

        @media (max-width: 768px) {
            .topic-grid, .side-by-side {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>📚 Guía Completa de Programación</h1>
            <p class="subtitle">Java - POO Completo | Python - Fundamentos Esenciales</p>
        </header>

        <nav>
            <button onclick="showSection('java')">☕ Java POO</button>
            <button onclick="showSection('python')">🐍 Python</button>
        </nav>

        <div class="content">
            <!-- ========== SECCIÓN JAVA ========== -->
            <div id="java" class="section active">
                <h2>☕ JAVA - Programación Orientada a Objetos</h2>

                <!-- 1. Paradigma de Clase -->
                <h3>1️⃣ Paradigma de Clase (POO)</h3>
                <div class="side-by-side">
                    <div>
                        <div class="code-block">
<span class="comment">// Clase: plantilla para crear objetos</span>
<span class="keyword">public class</span> <span class="function">Persona</span> {
    <span class="comment">// Atributos (características)</span>
    <span class="keyword">String</span> nombre;
    <span class="keyword">int</span> edad;

    <span class="comment">// Métodos (comportamientos)</span>
    <span class="keyword">public void</span> saludar() {
        System.out.println(<span class="string">"Hola, soy "</span> + nombre);
    }
}

<span class="comment">// Crear objeto (instancia)</span>
Persona persona1 = <span class="keyword">new</span> Persona();
persona1.nombre = <span class="string">"Juan"</span>;
persona1.edad = <span class="number">25</span>;
persona1.saludar();</div>
                    </div>
                    <div>
                        <div class="explanation">
                            <strong>💡 Concepto:</strong><br>
                            La POO organiza el código en <span class="highlight">clases</span> (plantillas) y <span class="highlight">objetos</span> (instancias).<br><br>
                            <strong>Ventajas:</strong><br>
                            • Reutilización de código<br>
                            • Modularidad<br>
                            • Modelar entidades reales
                        </div>
                    </div>
                </div>

                <!-- 2. Métodos y Atributos -->
                <h3>2️⃣ Métodos y Atributos</h3>
                <div class="side-by-side">
                    <div>
                        <div class="code-block">
<span class="keyword">public class</span> <span class="function">Coche</span> {
    <span class="comment">// ATRIBUTOS (estado)</span>
    <span class="keyword">String</span> marca;
    <span class="keyword">String</span> modelo;
    <span class="keyword">int</span> velocidad;

    <span class="comment">// MÉTODOS (comportamiento)</span>
    <span class="keyword">public void</span> acelerar(<span class="keyword">int</span> incremento) {
        velocidad += incremento;
        System.out.println(<span class="string">"Velocidad: "</span> +
                         velocidad + <span class="string">" km/h"</span>);
    }

    <span class="keyword">public void</span> frenar(<span class="keyword">int</span> decremento) {
        velocidad -= decremento;
        <span class="keyword">if</span>(velocidad < <span class="number">0</span>) velocidad = <span class="number">0</span>;
    }
}</div>
                    </div>
                    <div>
                        <div class="explanation">
                            <strong>📦 Atributos:</strong><br>
                            Variables que almacenan el estado del objeto<br><br>
                            <strong>⚙️ Métodos:</strong><br>
                            Funciones que definen el comportamiento
                        </div>
                    </div>
                </div>

                <!-- 3. Visibilidad -->
                <h3>3️⃣ Modificadores de Visibilidad</h3>
                <div class="side-by-side">
                    <div>
                        <div class="code-block">
<span class="keyword">public class</span> <span class="function">CuentaBancaria</span> {
    <span class="keyword">public</span> String titular;
    <span class="keyword">private</span> <span class="keyword">double</span> saldo;
    <span class="keyword">protected</span> String numeroCuenta;

    <span class="keyword">public void</span> depositar(<span class="keyword">double</span> cantidad) {
        saldo += cantidad;
    }

    <span class="keyword">public double</span> getSaldo() {
        <span class="keyword">return</span> saldo;
    }
}</div>
                    </div>
                    <div>
                        <div class="explanation">
                            <strong>🔒 Niveles de acceso:</strong><br><br>
                            <span class="highlight">public</span><br>
                            Acceso desde cualquier lugar<br><br>
                            <span class="highlight">private</span><br>
                            Solo dentro de la clase<br><br>
                            <span class="highlight">protected</span><br>
                            Clase, subclases y paquete
                        </div>
                    </div>
                </div>

                <!-- 4. Encapsulamiento -->
                <h3>4️⃣ Encapsulamiento (Getters y Setters)</h3>
                <div class="side-by-side">
                    <div>
                        <div class="code-block">
<span class="keyword">public class</span> <span class="function">Estudiante</span> {
    <span class="keyword">private</span> String nombre;
    <span class="keyword">private</span> <span class="keyword">int</span> edad;
    <span class="keyword">private</span> <span class="keyword">double</span> promedio;

    <span class="comment">// GETTER: obtener valor</span>
    <span class="keyword">public</span> String getNombre() {
        <span class="keyword">return</span> nombre;
    }

    <span class="comment">// SETTER: establecer con validación</span>
    <span class="keyword">public void</span> setNombre(String nombre) {
        <span class="keyword">if</span>(nombre != <span class="keyword">null</span> &&
           !nombre.isEmpty()) {
            <span class="keyword">this</span>.nombre = nombre;
        }
    }

    <span class="keyword">public void</span> setEdad(<span class="keyword">int</span> edad) {
        <span class="keyword">if</span>(edad > <span class="number">0</span> && edad < <span class="number">150</span>) {
            <span class="keyword">this</span>.edad = edad;
        }
    }
}</div>
                    </div>
                    <div>
                        <div class="explanation">
                            <strong>🛡️ Protección de datos:</strong><br><br>
                            • Atributos privados<br>
                            • Acceso controlado<br>
                            • Validación de datos<br>
                            • Mayor seguridad
                        </div>
                    </div>
                </div>

                <!-- 5. Constructores -->
                <h3>5️⃣ Constructor y Sobrecarga</h3>
                <div class="side-by-side">
                    <div>
                        <div class="code-block">
<span class="keyword">public class</span> <span class="function">Producto</span> {
    <span class="keyword">private</span> String nombre;
    <span class="keyword">private</span> <span class="keyword">double</span> precio;
    <span class="keyword">private</span> <span class="keyword">int</span> stock;

    <span class="comment">// Constructor por defecto</span>
    <span class="keyword">public</span> Producto() {
        <span class="keyword">this</span>.nombre = <span class="string">"Sin nombre"</span>;
        <span class="keyword">this</span>.precio = <span class="number">0.0</span>;
        <span class="keyword">this</span>.stock = <span class="number">0</span>;
    }

    <span class="comment">// Constructor con parámetros</span>
    <span class="keyword">public</span> Producto(String nombre,
                     <span class="keyword">double</span> precio) {
        <span class="keyword">this</span>.nombre = nombre;
        <span class="keyword">this</span>.precio = precio;
        <span class="keyword">this</span>.stock = <span class="number">0</span>;
    }

    <span class="comment">// Constructor completo</span>
    <span class="keyword">public</span> Producto(String nombre,
                     <span class="keyword">double</span> precio,
                     <span class="keyword">int</span> stock) {
        <span class="keyword">this</span>.nombre = nombre;
        <span class="keyword">this</span>.precio = precio;
        <span class="keyword">this</span>.stock = stock;
    }
}</div>
                    </div>
                    <div>
                        <div class="explanation">
                            <strong>🏗️ Constructores:</strong><br><br>
                            Inicializan objetos al crearlos<br><br>
                            <strong>Sobrecarga:</strong><br>
                            Múltiples constructores con diferentes parámetros
                        </div>
                    </div>
                </div>

                <!-- 6. Herencia y Agregación -->
                <h3>6️⃣ Herencia y Agregación</h3>
                <div class="topic-grid">
                    <div class="topic-item">
                        <h4 style="color: #60a5fa; margin-bottom: 10px;">🧬 Herencia</h4>
                        <div class="code-block">
<span class="keyword">public class</span> <span class="function">Persona</span> {
    <span class="keyword">protected</span> String nombre;
    <span class="keyword">protected</span> <span class="keyword">int</span> edad;

    <span class="keyword">public void</span> presentarse() {
        System.out.println(nombre);
    }
}

<span class="keyword">public class</span> <span class="function">Empleado</span> <span class="keyword">extends</span> Persona {
    <span class="keyword">private</span> <span class="keyword">double</span> salario;

    <span class="keyword">@Override</span>
    <span class="keyword">public void</span> presentarse() {
        System.out.println(nombre +
            <span class="string">", empleado"</span>);
    }
}</div>
                        <div class="explanation">
                            Relación <span class="highlight">"es un"</span><br>
                            Empleado ES UNA Persona
                        </div>
                    </div>

                    <div class="topic-item">
                        <h4 style="color: #60a5fa; margin-bottom: 10px;">📦 Agregación</h4>
                        <div class="code-block">
<span class="keyword">public class</span> <span class="function">Empresa</span> {
    <span class="keyword">private</span> String nombre;
    <span class="keyword">private</span> List&lt;Empleado&gt; empleados;

    <span class="keyword">public</span> Empresa(String nombre) {
        <span class="keyword">this</span>.nombre = nombre;
        empleados = <span class="keyword">new</span> ArrayList&lt;&gt;();
    }

    <span class="keyword">public void</span> agregarEmpleado(
                        Empleado e) {
        empleados.add(e);
    }
}</div>
                        <div class="explanation">
                            Relación <span class="highlight">"tiene un"</span><br>
                            Empresa TIENE Empleados
                        </div>
                    </div>
                </div>

                <!-- 7. Clases Abstractas -->
                <h3>7️⃣ Clases y Métodos Abstractos</h3>
                <div class="side-by-side">
                    <div>
                        <div class="code-block">
<span class="keyword">public abstract class</span> <span class="function">Figura</span> {
    <span class="keyword">protected</span> String color;

    <span class="comment">// Método abstracto</span>
    <span class="keyword">public abstract double</span> calcularArea();

    <span class="comment">// Método concreto</span>
    <span class="keyword">public void</span> mostrarColor() {
        System.out.println(color);
    }
}

<span class="keyword">public class</span> <span class="function">Circulo</span> <span class="keyword">extends</span> Figura {
    <span class="keyword">private</span> <span class="keyword">double</span> radio;

    <span class="keyword">@Override</span>
    <span class="keyword">public double</span> calcularArea() {
        <span class="keyword">return</span> Math.PI * radio * radio;
    }
}

<span class="keyword">public class</span> <span class="function">Rectangulo</span> <span class="keyword">extends</span> Figura {
    <span class="keyword">private</span> <span class="keyword">double</span> base, altura;

    <span class="keyword">@Override</span>
    <span class="keyword">public double</span> calcularArea() {
        <span class="keyword">return</span> base * altura;
    }
}</div>
                    </div>
                    <div>
                        <div class="explanation">
                            <strong>🎨 Clase Abstracta:</strong><br><br>
                            • No se puede instanciar<br>
                            • Define plantilla<br>
                            • Métodos sin implementar<br>
                            • Subclases deben implementar
                        </div>
                    </div>
                </div>

                <!-- 8. Final -->
                <h3>8️⃣ Modificador Final</h3>
                <div class="topic-grid">
                    <div class="topic-item">
                        <h4 style="color: #60a5fa;">Variables Final</h4>
                        <div class="code-block">
<span class="keyword">public class</span> <span class="function">Constantes</span> {
    <span class="keyword">public static final double</span> PI =
        <span class="number">3.14159</span>;

    <span class="keyword">private final</span> String id;

    <span class="keyword">public</span> Constantes(String id) {
        <span class="keyword">this</span>.id = id;
    }
}</div>
                        <div class="explanation">
                            Valores constantes que no cambian
                        </div>
                    </div>

                    <div class="topic-item">
                        <h4 style="color: #60a5fa;">Métodos y Clases Final</h4>
                        <div class="code-block">
<span class="keyword">public class</span> <span class="function">Padre</span> {
    <span class="comment">// No se puede sobrescribir</span>
    <span class="keyword">public final void</span> metodoFinal() {
        System.out.println(<span class="string">"Final"</span>);
    }
}

<span class="comment">// No se puede heredar</span>
<span class="keyword">public final class</span> <span class="function">Matematicas</span> {
    <span class="keyword">public static int</span> sumar(
                       <span class="keyword">int</span> a, <span class="keyword">int</span> b) {
        <span class="keyword">return</span> a + b;
    }
}</div>
                        <div class="explanation">
                            Previene modificación y herencia
                        </div>
                    </div>
                </div>

                <!-- 9. Interfaces -->
                <h3>9️⃣ Interfaces</h3>
                <div class="side-by-side">
                    <div>
                        <div class="code-block">
<span class="keyword">public interface</span> <span class="function">Volador</span> {
    <span class="keyword">void</span> volar();
    <span class="keyword">void</span> aterrizar();
}

<span class="keyword">public interface</span> <span class="function">Nadador</span> {
    <span class="keyword">void</span> nadar();
}

<span class="keyword">public class</span> <span class="function">Pato</span>
       <span class="keyword">implements</span> Volador, Nadador {

    <span class="keyword">@Override</span>
    <span class="keyword">public void</span> volar() {
        System.out.println(
            <span class="string">"El pato vuela"</span>);
    }

    <span class="keyword">@Override</span>
    <span class="keyword">public void</span> aterrizar() {
        System.out.println(
            <span class="string">"El pato aterriza"</span>);
    }

    <span class="keyword">@Override</span>
    <span class="keyword">public void</span> nadar() {
        System.out.println(
            <span class="string">"El pato nada"</span>);
    }
}</div>
                    </div>
                    <div>
                        <div class="explanation">
                            <strong>📋 Contratos:</strong><br><br>
                            • Define comportamientos<br>
                            • Herencia múltiple<br>
                            • Clases implementan<br>
                            • Polimorfismo
                        </div>
                    </div>
                </div>

                <!-- 10. Excepciones -->
                <h3>🔟 Manejo de Excepciones</h3>
                <div class="side-by-side">
                    <div>
                        <div class="code-block">
<span class="keyword">public class</span> <span class="function">Division</span> {

    <span class="keyword">public static int</span> dividir(<span class="keyword">int</span> a,
                             <span class="keyword">int</span> b)
           <span class="keyword">throws</span> ArithmeticException {
        <span class="keyword">if</span>(b == <span class="number">0</span>) {
            <span class="keyword">throw new</span> ArithmeticException(
                <span class="string">"División por cero"</span>);
        }
        <span class="keyword">return</span> a / b;
    }

    <span class="keyword">public static void</span> main(String[] args) {
        <span class="keyword">try</span> {
            <span class="keyword">int</span> resultado = dividir(<span class="number">10</span>, <span class="number">0</span>);
            System.out.println(resultado);

        } <span class="keyword">catch</span>(ArithmeticException e) {
            System.out.println(
                <span class="string">"Error: "</span> + e.getMessage());

        } <span class="keyword">finally</span> {
            System.out.println(<span class="string">"Fin"</span>);
        }
    }
}

<span class="comment">// Excepción personalizada</span>
<span class="keyword">public class</span> <span class="function">EdadInvalidaException</span>
       <span class="keyword">extends</span> Exception {
    <span class="keyword">public</span> EdadInvalidaException(
                          String msg) {
        <span class="keyword">super</span>(msg);
    }
}</div>
                    </div>
                    <div>
                        <div class="explanation">
                            <strong>⚠️ Gestión de errores:</strong><br><br>
                            <span class="highlight">try</span><br>
                            Código a probar<br><br>
                            <span class="highlight">catch</span><br>
                            Captura error<br><br>
                            <span class="highlight">finally</span><br>
                            Siempre se ejecuta<br><br>
                            <span class="highlight">throws</span><br>
                            Declara excepción
                        </div>
                    </div>
                </div>

                <!-- 11. Hilos -->
                <h3>1️⃣1️⃣ Hilos (Threads)</h3>
                <div class="topic-grid">
                    <div class="topic-item">
                        <h4 style="color: #60a5fa;">Extender Thread</h4>
                        <div class="code-block">
<span class="keyword">public class</span> <span class="function">MiHilo</span>
       <span class="keyword">extends</span> Thread {
    <span class="keyword">private</span> String nombre;

    <span class="keyword">public</span> MiHilo(String nombre) {
        <span class="keyword">this</span>.nombre = nombre;
    }

    <span class="keyword">@Override</span>
    <span class="keyword">public void</span> run() {
        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>; i<=<span class="number">5</span>; i++) {
            System.out.println(
                nombre + <span class="string">": "</span> + i);
            <span class="keyword">try</span> {
                Thread.sleep(<span class="number">1000</span>);
            } <span class="keyword">catch</span>(InterruptedException e){}
        }
    }
}

<span class="comment">// Uso</span>
MiHilo h1 = <span class="keyword">new</span> MiHilo(<span class="string">"Hilo-1"</span>);
h1.start();</div>
                    </div>

                    <div class="topic-item">
                        <h4 style="color: #60a5fa;">Implementar Runnable</h4>
                        <div class="code-block">
<span class="keyword">public class</span> <span class="function">Tarea</span>
       <span class="keyword">implements</span> Runnable {
    <span class="keyword">private</span> String nombre;

    <span class="keyword">public</span> Tarea(String nombre) {
        <span class="keyword">this</span>.nombre = nombre;
    }

    <span class="keyword">@Override</span>
    <span class="keyword">public void</span> run() {
        System.out.println(
            <span class="string">"Ejecutando: "</span> + nombre);
    }
}

<span class="comment">// Uso</span>
Thread hilo = <span class="keyword">new</span> Thread(
    <span class="keyword">new</span> Tarea(<span class="string">"Tarea-1"</span>));
hilo.start();</div>
                    </div>
                </div>
                <div class="explanation">
                    <strong>🔄 Concurrencia:</strong> Los hilos permiten ejecutar múltiples tareas simultáneamente. Útil para operaciones en segundo plano, interfaces responsivas y procesamiento paralelo.
                </div>

                <!-- 12. Listas -->
                <h3>1️⃣2️⃣ Listas (Collections)</h3>
                <div class="side-by-side">
                    <div>
                        <div class="code-block">
<span class="keyword">import</span> java.util.*;

<span class="keyword">public class</span> <span class="function">EjemploListas</span> {
    <span class="keyword">public static void</span> main(String[] args) {

        <span class="comment">// ArrayList</span>
        List&lt;String&gt; frutas =
            <span class="keyword">new</span> ArrayList&lt;&gt;();

        frutas.add(<span class="string">"Manzana"</span>);
        frutas.add(<span class="string">"Banana"</span>);
        frutas.add(<span class="string">"Naranja"</span>);

        System.out.println(frutas.get(<span class="number">0</span>));
        frutas.set(<span class="number">1